#ifndef ELIMINACION_H // Comprueba si ELIMINACION_H no ha sido definido previamente.
#define ELIMINACION_H // Define ELIMINACION_H para evitar inclusiones múltiples del mismo archivo

void eliminarRegistro(int idEliminar); // Declaración de la función eliminarRegistro, que toma un ID como argumento para eliminar el registro correspondiente de una base de datos o archivo

#endif // Fin del bloque de inclusión para ELIMINACION_H

#pragma once // Directiva que también previene inclusiones múltiples del mismo archivo; sin embargo, no es necesaria si ya se ha usado #ifndef